# Database PostgreSQL
# Use DATABASE_URL para connection string (ex: NeonDB)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Ou use as variáveis individuais (para desenvolvimento local)
DB_HOST=localhost
DB_USER=postgres
DB_PASS=postgres
DB_NAME=postgres

# Database MongoDB
MONGO_URL=mongodb://localhost:27017/devburger

# JWT
JWT_SECRET=sua-chave-secreta-super-segura
JWT_EXPIRES_IN=7d

# Stripe
STRIPE_SECRET_KEY=sk_test_sua-chave-stripe

# Server
PORT=3001

# Public URL da API (incluindo protocolo)
# Ex: http://localhost:3001 em dev, https://sua-api.com em produção
APP_URL=http://localhost:3001